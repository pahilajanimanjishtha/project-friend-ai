[project]
name = "video-avatar-demo"
version = "0.1.0"
description = "Realtime video avatar of yourself, powered by Tavus CVI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
